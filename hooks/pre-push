#!/bin/sh
# THE PUSH STAGE, AS THE PUSH HOOK (CA master-plan F15): `just gate`.
#
# The hook is one line and never changes. What a push must pass is the `gate`
# recipe in stages.just beside the justfile — the same recipe a session runs by
# hand.
exec just gate

# Tasks: The export hatch

- [X] T001 Test-first (wire, `__tests__/mcp-documents.test.ts`): dissolve →
      export_note → markdown equals blocks joined `\n\n`; separator-split
      re-import matches count+order; miss shape; fence → 26 + read-only
      annotation in `__tests__/mcp-http.test.ts`. Run: red.
- [X] T002 Register `export_note` in `src/mcp/server.ts`. Run: green.
- [X] T003 Gate + audit — green. Land. F14's grace half recorded as B1's
      mount (node disposition).

# Caller stub (Flame pull-CI, frontend) — logic in foundry/foundry-stocks; invariant, copier-shipped.
name: ci
on:
  pull_request:
jobs:
  ci:
    uses: foundry/foundry-stocks/.forgejo/workflows/frontend-ci.yml@main
    secrets: inherit
